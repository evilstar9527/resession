{
  "name": "resession-server",
  "version": "0.2.0",
  "private": true,
  "description": "Sync server for resession — stores Claude/Codex session metadata + JSONL for cross-device viewing.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.0.0"
  }
}
